(function() {
    'user strict';
    angular
        .module("FormBuilderServerApp")
        .factory("FormService", FormService);

    function FormService($http, $q) {
        var api = {

        };

        return api;
    }
})();